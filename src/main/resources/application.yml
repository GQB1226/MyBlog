security:
  basic:
    enabled: true
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/blog
    username:
    password:
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  thymeleaf:
    mode: HTML5
server:
  port: 8080
qiniu:
  accessKey:
  secretKey:
  bucketName: gqbit
  basePath: 